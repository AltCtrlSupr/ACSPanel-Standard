#!/bin/bash
# prerm script for symfony2-site-zaspanel-main
#
# see: dh_installdeb(1)

set -e

# summary of how this script can be called:
#        * <prerm> `remove'
#        * <old-prerm> `upgrade' <new-version>
#        * <new-prerm> `failed-upgrade' <old-version>
#        * <conflictor's-prerm> `remove' `in-favour' <package> <new-version>
#        * <deconfigured's-prerm> `deconfigure' `in-favour'
#          <package-being-installed> <version> `removing'
#          <conflicting-package> <version>
# for details, see http://www.debian.org/doc/debian-policy/ or
# the debian-policy package

. /usr/share/debconf/confmodule


. /usr/share/dbconfig-common/dpkg/prerm

# Run dbconfig-common
dbc_go symfony2-site-zaspanel-main $@


case "$1" in
    remove|deconfigure)

    rm -f /etc/packaged-site/symfony2-site-zaspanel-main/dynamic.yml
    rm -f /var/www/symfony2-site-zaspanel-main/app/main/config/dynamic.yml
    rm -f /var/www/symfony2-site-zaspanel-main/app/main/config/dynamic.yml.2
    rm -f /var/www/symfony2-site-zaspanel-main/app/main/config/dynamic.yml.3


        . /usr/share/packaged-site/symfony2-site-zaspanel-main/symfony2-site-zaspanel-main.confmodule

        rm -f /etc/apache2/sites-enabled/110-symfony2-site-zaspanel-main-100-redirects.conf
        rm -f /etc/apache2/sites-available/110-symfony2-site-zaspanel-main-100-redirects.conf

        rm -f /etc/apache2/sites-enabled/110-symfony2-site-zaspanel-main-101-primary.conf
        rm -f /etc/apache2/sites-available/110-symfony2-site-zaspanel-main-101-primary.conf

        rm -f /etc/apache2/sites-enabled/110-symfony2-site-zaspanel-main-102-secondary.conf
        rm -f /etc/apache2/sites-available/110-symfony2-site-zaspanel-main-102-secondary.conf

        rm -f /etc/packaged-site/symfony2-site-zaspanel-main/apachedir
        rm -f /etc/packaged-site/symfony2-site-zaspanel-main/appapachesettings
        rm -f /etc/packaged-site/symfony2-site-zaspanel-main/htpassd

        rm -fr /var/www/symfony2-site-zaspanel-main/app/main/cache/

        graceful_apache2;


    ;;
    upgrade)
    ;;
    failed-upgrade)
    ;;

    *)
        echo "prerm called with unknown argument \`$1'" >&2
        exit 1
    ;;
esac

#DEBHELPER#

exit 0


