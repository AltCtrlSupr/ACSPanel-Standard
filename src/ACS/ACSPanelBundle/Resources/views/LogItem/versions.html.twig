{% extends '::base.html.twig' %}
{% block header_title %} - {% trans %}logitem edit{% endtrans %}{% endblock %}
{% block body %}

<div class="form_box">
    <form action="{{ path('logitem_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <p>
            <button type="submit">{%trans%}Edit{%endtrans%}</button>
        </p>
    </form>
</div>

<ul class="record_actions">
    <li>
        <a href="{{ path('logitem') }}">
            {%trans%}Back to the list{%endtrans%}
        </a>
    </li>
</ul>

{% endblock %}
