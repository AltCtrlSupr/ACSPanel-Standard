{% extends '::base.html.twig' %}
{% block header_title %} - {% trans %}DnsDomain creation{% endtrans %}{% endblock %}
{% block body %}

<div class="form_box">
    <form action="{{ path('dnsdomain_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">{%trans%}Create{%endtrans%}</button>
        </p>
    </form>
</div>

<ul class="record_actions">
    <li>
        <a href="{{ path('dnsdomain') }}">
            {%trans%}Back to the list{%endtrans%}
        </a>
    </li>
</ul>

{% endblock %}
